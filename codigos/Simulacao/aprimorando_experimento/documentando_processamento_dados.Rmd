---
title: "R Notebook"
output: html_notebook
---

# Imputação da idade do pai - caso de estudo Paraná

Pacotes e configurações necessárias
Há época, foi preciso instalar a ver 4.2 do Rtools para proceder com a instalacao dos pacotes fora do CRAN:
- windows: https://cran.r-project.org/bin/windows/Rtools/rtools42/rtools.html

- origem:
https://github.com/danicat/read.dbc
https://github.com/rfsaldanha/microdatasus
```{r}
devtools::install_github("danicat/read.dbc")
remotes::install_github("rfsaldanha/microdatasus")
```

Carrega pacotes necessários:
```{r}
library(dplyr)
require(tidyverse)
library(ggplot2)
library(mice)
library(stringr)
library(ggmice)
library(lubridate)
library(readxl)
library(hms)

options(scipen = 999, 
        OutDec = ",")
```

## Buscando dados:
conseguindo os dados e salvando o df:
 - Extração
Faz requisição ao ftp datasus das variáveis selecionadas
```{r}

UFs = c("AC", "AL", "AP", "AM", "BA", "CE", "DF", "ES", "GO", "MA", "MT", "MS", "MG", "PA", "PB", "PR", "PE", "PI", "RJ", "RN", "RS", "RO", "RR", "SC", "SP", "SE", "TO")

for (UF in UFs) {
  nome <- paste0("Dados_",UF)
  name=assign(nome, fetch_datasus(year_start = 2012, year_end = 2022, uf = UF, information_system = "SINASC", vars = c("IDADEMAE", "IDADEPAI", "PARTO", "ESCMAE","ESTCIVMAE","DTRECEBIM", "DTNASC", "HORANASC","CODESTAB","TPFUNCRESP","RACACORMAE")))
  save(name,file=paste0(UF, ".Rdata"))
  rm(name)
}
```


Faz processamento dos dados: 

A função \code{processes_sinasc()} processa variáveis do SINASC retornadas pela função \code{fetch_datasus()}, atribuindo legendas para variáveis categóricas, incluindo valores NA.
Fontes:
[process_sinasc.R](https://github.com/rfsaldanha/microdatasus/blob/master/man/process_sinasc.Rd)
e [process_sinasc.R](https://github.com/rfsaldanha/microdatasus/blob/master/R/process_sinasc.R)
Altera a idade do pai desta forma:
```{r,eval=FALSE, warning=FALSE, message=FALSE}
  # IDADEPAI
  if("IDADEPAI" %in% variables_names){
    data <- data %>%
      dplyr::mutate(IDADEPAI = dplyr::case_match(
        .data$IDADEPAI,
        "0" ~ NA,
        "99" ~ NA,
        .default = .data$IDADEPAI
      )) %>%
      dplyr::mutate(IDADEPAI = as.numeric(.data$IDADEPAI))
  }
```

Itera sobre cada estado para processar e salvar dados
```{r, eval=FALSE, warning=FALSE, message=FALSE}

Brasil_processed <- list()

for (uf in UFs) {

  # Aplica a função process_sinasc ao data frame carregado
  uf_processed <- process_sinasc(get(paste0("Dados_",uf)), municipality_data = TRUE)

  # Armazena o data frame processado na lista Brasil_processed
  Brasil_processed[[uf]] <- uf_processed

  # Remove o data frame temporário para liberar memória
  rm(list = uf)
}

names(Brasil_processed$AC)

```

Separa os dados por região para poder ficar mais leve de carregar no ambiente
```{r}

# Usar lapply para adicionar a coluna de região
Brasil_processed <- lapply(Brasil_processed, function(df) {
  df %>%
    mutate(Regiao_de_registro_do_nascimento = substr(CODMUNNASC, 1, 1))
})

# Inicializar a lista 'regioes'
regioes <- list()

# Agrupar os dados em uma lista por região
for (regiao in unique(unlist(lapply(Brasil_processed, function(df) df$Regiao_de_registro_do_nascimento)))) {
  regioes[[regiao]] <- bind_rows(
    lapply(Brasil_processed, function(df) {
      df %>% filter(Regiao_de_registro_do_nascimento == regiao)
    })
  )
}

# Salvar cada região como um novo objeto na sessão do R
list2env(regioes, envir = .GlobalEnv)
```


Avaliando a documentação. Esse trecho visualiza a sessão da DN onde estão presentes as categorias associadas ao preenchimento da DN:

```{r, eval=FALSE}
#Estrutura_SINASC <- read_excel("/imputacao_para_idade_do_pai/manuais/Estrutura_SINASC_para_CD.xlsx")
#View(Estrutura_SINASC_para_CD)
ver = Estrutura_SINASC %>% 
  filter(`Sessão da DN`== "VII -\r\nPreenchimento") %>% 
  print()


```

```{r, eval=FALSE}
ver = Estrutura_SINASC %>% 
  filter(`Nome do campo no DBF`== "QTDGESTANT") %>% 
  print()

```


## Preparando dados: 

```{r}
# Iterar sobre as regiões e aplicar transformações
resultados <- lapply(regioes, function(regiao) {
  regiao <- regiao %>%
    mutate(
      Ano = year(as.Date(DTNASC)), # Extrai o ano de nascimento
      missing = ifelse(is.na(IDADEPAI), 1, 0), # Identifica valores ausentes
    )
})

```

Cortando para o intervalo de referência do período repodrodutivo
```{r}
resultados <- lapply(regioes, function(regiao) {
  regiao <- regiao %>%
    mutate(IDADEMAE = as.numeric(IDADEMAE),
           IDADEPAI = as.numeric(IDADEPAI)) %>% 
    filter(
    (IDADEMAE >= 15 & IDADEMAE < 50),
    (IDADEPAI >= 15 & IDADEPAI < 60)) %>%
     mutate(
    # Filtra valores inválidos ou transforma em NA
    HORANASC = ifelse(
      grepl("^[0-9]{1,4}$", HORANASC), # Verifica se HORANASC contém apenas números (1-4 dígitos)
      str_pad(HORANASC, width = 4, pad = "0"), # Preenche com zeros à esquerda se necessário
      NA_character_ # Define como NA se for inválido
    )
  ) %>%
  mutate(
    HORANASC = ifelse(
      is.na(HORANASC),
      NA, # Mantém NA para valores inválidos
      parse_hms(paste0(substr(HORANASC, 1, 2), ":", substr(HORANASC, 3, 4), ":00"))
    ),
    HORANASC = hms::hms(HORANASC),
    DTNASC = as.Date(DTNASC, format = "%Y-%m-%d"),
    DTRECEBIM = as.Date(DTRECEBIM , format = "%Y-%m-%d")
    )
})
```


##### Adequa para construção do modelo
```{r, eval=FALSE, include=FALSE}
# Converter HORANASC em segundos do dia

resultados <- lapply(regioes, function(regiao) {
  regiao <- regiao %>%
    mutate( HORANASC_seconds = as.numeric(as_hms(HORANASC)),
          HORANASC_seconds_binned = cut(
      HORANASC_seconds,
      breaks = c(0, 21600, 43200, 64800, 86400), 
      labels = c("Madrugada", "Manhã", "Tarde", "Noite"),
      include.lowest = TRUE
    ),
    Diferenca_nasc_receb = (DTNASC, DTRECEBIM) 
) %>%
    mutate(
    ESCMAE =as.factor(ESCMAE)
    LOCNASC = as.factor(LOCNASC),
    ESTCIVMAE = as.factor(ESTCIVMAE),
    RACACORMAE = as.factor(RACACORMAE),
    TPFUNCRESP = as.factor(TPFUNCRESP),
    CODESTAB = as.factor(CODESTAB)
  )
})


# Converte para factors para facilitar o processamento
Parana <- Parana %>%


```



### Calcula a probabilidade da idade do pai ser ausente de acordo com a idade da mae (e outros preditores):

#### Regressão Logística
```{r, eval=FALSE, include=FALSE}
# Carregar os pacotes necessários
library(tidyr)   # Para manipulação de dados aninhados (nest, unnest)
library(purrr)   # Para aplicar funções de forma funcional (map)
library(broom)   # Para extrair estatísticas de modelos de regressão

# Criar um modelo GLM para prever a ausência (missing) de IDADEPAI baseado em IDADEMAE por Ano
pai_mae_glm <- Parana |> 
  select(IDADEPAI, IDADEMAE, missing, Ano) |>  # Selecionar apenas as variáveis relevantes
  group_by(Ano) |>  # Agrupar os dados por Ano
  nest() |>  # Transformar os dados de cada ano em uma lista separada (um "mini data frame" por ano)
  
  mutate(
    # Criar um modelo GLM (regressão logística) para cada Ano
    glm_model = map(data, ~glm(missing ~ IDADEMAE, 
                               family = binomial, data = .x)), 
    
    # Extrair os coeficientes do modelo de cada ano usando a função `tidy()` do pacote `broom`
    glm_summary = map(glm_model, tidy)
  ) |> 

  # Selecionar apenas o Ano e o resumo do modelo (coeficientes)
  select(Ano, glm_summary) |> 

  # Expandir os coeficientes para que cada linha represente um coeficiente do modelo de cada ano
  unnest(glm_summary)

# Exibir os resultados
print(pai_mae_glm)

```



```{r}

```

