---
title: "visualizacoes_artigo"
author: "mimi"
date: "2026-05-07"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}

library(here)
library(showtext)
library(writexl)

font_add("Arial", paste0(here(),"/artigo/arial.ttf")) 
showtext_auto()
```


```{r}

load(paste0(here(),"/codigos/resultados_todos_20250706.RData"))

```


tabela_po_idade  > RMSE_TEFM
tabelas_por_idade_erro > RB_TEFM


tabela_larga_erro > RB_TFTM
tabela_larga > RMSE_TFTM
tabela_larga_PB > PB_TFT


```{r}


library(tidyverse)
library(stringr)
library(ggplot2)

dados_plot <- tabelas_por_idade %>%
  bind_rows(.id = "faixa_idade") %>%
  pivot_longer(
    cols = -c(faixa_idade, mecanismo, proporcao),
    names_to = "Imputation method",
    values_to = "valor"
  ) %>%
  mutate(
    `REQM TEFM` = as.numeric(str_replace(valor, ",", ".")),
    cenario = paste(mecanismo, proporcao)
  ) %>%
  select(-valor) %>% 
   mutate(
    `Imputation method` = case_when(
      `Imputation method` == "Mediana" ~ "Median",
      `Imputation method` == "Média" ~ "Mean",
      `Imputation method` == "Mediana condicional\na idade da mãe" ~ "MCMA", # Median conditional on maternal age
      `Imputation method` == "Mediana condicional\n a idade da mãe\ne ao estado civil" ~ "MCMAMS", # Median conditional on maternal age and marital status
      `Imputation method` == "Mediana condicional\n a idade da mãe\ne ao turno de nascimento" ~ "MCMABS",#Median conditional on maternal age and birth shift;
      `Imputation method` == "Mediana condicional \na idade da mãe\ne a função do responsável\npelo preenchimento da DN" ~ "MCMARR", # Median conditional on maternal age and the role of the person responsible for completing the Live Birth Declaration
      `Imputation method` == "Análise de casos completos"~ "CCA", #COMPLETE CASE ANALISES
      TRUE ~ `Imputation method`
    )
  ) %>% 
  rename( "Age group" = faixa_idade,
          Percentage = proporcao,
          Mecanism = mecanismo, 
          Setting = cenario
          ) %>% 
  mutate(
    Mecanism = case_when(
      Mecanism == "MA" ~ "MAR",
      Mecanism == "MCA" ~ "MCAR",
      Mecanism == "MNA" ~ "MNAR"
  ),
   Setting = Setting %>%
      str_replace("^MA", "MAR") %>%
      str_replace("^MCA", "MCAR") %>%
      str_replace("^MNA", "MNAR"))

REQM_TEFM = ggplot(dados_plot,
       aes(x =`Age group`,
           y =  `Imputation method`,
           fill = `REQM TEFM`)) +
  geom_tile(color = "white") +
  labs(title = "REQM TEFM",
       y= "Imputation method",
       x= "Age group")+
  facet_wrap(~ Setting) +
  scale_fill_gradient2(
    low = "#B2182B",    # negativos
    mid = "white",      # próximo de 0
    high = "#2166AC",   # positivos
    midpoint = 0,
    name = "REQM"
  ) +
  theme_minimal(base_family = "Arial")+
  theme(
    axis.text.x = element_text(angle = 90, hjust = 0.5),
    plot.title = element_text(hjust = 0.5)
  )

ggsave("REQM_TEFM.png",REQM_TEFM, width = 16.5, height = 12, units = "cm", path = paste0(here(), "/artigo"),  dpi=300)
ggsave("REQM_TEFM.pdf",REQM_TEFM, width = 16.5, height = 12, units = "cm", path = paste0(here(), "/artigo"),  dpi=300)
write_xlsx(dados_plot, paste0(here(), "/artigo", "/REQM_TEFM.xlsx"))
```




```{r}

dados_plot_erro <- tabelas_por_idade_erro %>%
  bind_rows(.id = "faixa_idade") %>%
  pivot_longer(
    cols = -c(faixa_idade, mecanismo, proporcao),
    names_to = "metodo",
    values_to = "valor"
  ) %>%
  mutate(
    valor = as.numeric(str_replace(valor, ",", ".")),
    cenario = paste(mecanismo, proporcao)
  ) %>%
   mutate(
    metodo = case_when(
      metodo == "Mediana condicional\na idade da mãe" ~ "MCIM",
      metodo == "Mediana condicional\n a idade da mãe\ne ao estado civil" ~ "MCIMEC",
      metodo == "Mediana condicional\n a idade da mãe\ne ao turno de nascimento" ~ "MCIMTN",
      metodo == "Mediana condicional \na idade da mãe\ne a função do responsável\npelo preenchimento da DN" ~ "MCIMFR",
      metodo == "Análise de casos completos"~ "ACC",
      TRUE ~ metodo
    )
  )

rb_TEFM = ggplot(dados_plot_erro,
       aes(x =faixa_idade,
           y =  metodo,
           fill = valor)) +
  geom_tile(color = "white") +
  labs(title = "Erro bruto TEFM",
       y= "Método de imputação",
       x= "Faixa etária")+
  facet_wrap(~ cenario) +
   scale_fill_gradient2(
    low = "#B2182B",    
    mid = "white",      
    high = "#2166AC",   
    midpoint = 0,
    name = "Erro bruto"
  ) +

  `theme_minimal(base_family = "Arial") +
  theme(
    axis.text.x = element_text(angle = 90, hjust = 0.5),
    plot.title = element_text(hjust = 0.5)
  )

ggsave("EB_TEFM.png",rb_TEFM, width = 15.5,
  height = 12, units = "cm", path = here(),  dpi=300)
```



```{r}

dados_plot_PB <- tabelas_por_idade_PB %>%
  bind_rows(.id = "faixa_idade") %>%
  pivot_longer(
    cols = -c(faixa_idade, mecanismo, proporcao),
    names_to = "metodo",
    values_to = "valor"
  ) %>%
  mutate(
    valor = as.numeric(str_replace(valor, ",", ".")),
    cenario = paste(mecanismo, proporcao)
  ) %>%
   mutate(
    metodo = case_when(
      metodo == "Mediana condicional\na idade da mãe" ~ "MCIM",
      metodo == "Mediana condicional\n a idade da mãe\ne ao estado civil" ~ "MCIMEC",
      metodo == "Mediana condicional\n a idade da mãe\ne ao turno de nascimento" ~ "MCIMTN",
      metodo == "Mediana condicional \na idade da mãe\ne a função do responsável\npelo preenchimento da DN" ~ "MCIMFR",
      metodo == "Análise de casos completos"~ "ACC",
      TRUE ~ metodo
    )
  )

PB_TEFM = ggplot(dados_plot_PB,
       aes(x =faixa_idade,
           y =  metodo,
           fill = valor)) +
  geom_tile(color = "white") +
  labs(title = "Viés relativo TEFM",
       y= "Método de imputação",
       x= "Faixa etária")+
  facet_wrap(~ cenario) +
   scale_fill_gradient2(
    low = "#B2182B",    
    mid = "white",      
    high = "#2166AC",   
    midpoint = 0,
    name = "Viés relativo"
  ) +

  `theme_minimal(base_family = "Arial") +
  theme(
    axis.text.x = element_text(angle = 90, hjust = 0.5),
    plot.title = element_text(hjust = 0.5)
  )

ggsave("PB_TEFM.png",PB_TEFM, width = 15.5,
  height = 12, units = "cm", path = here(),  dpi=300)
```








```{r}
library(patchwork)

padroniza_metodo <- function(df){

  df %>%
pivot_longer(
      cols = -c(mecanismo, proporcao),
      names_to = "metodo",
      values_to = "valor"
    ) %>%
mutate(
      valor = as.numeric(str_replace(valor, ",", ".")),

      metodo = case_when(
        metodo == "Mediana condicional\na idade da mãe" ~ "MCIM",
        metodo == "Mediana condicional\n a idade da mãe\ne ao estado civil" ~ "MCIMEC",
        metodo == "Mediana condicional\n a idade da mãe\ne ao turno de nascimento" ~ "MCIMTN",
        metodo == "Mediana condicional \na idade da mãe\ne a função do responsável\npelo preenchimento da DN" ~ "MCIMFR",
        metodo == "Análise de casos completos" ~ "ACC",
        TRUE ~ metodo
      )
    ) |> 
    filter( metodo != "ACC")
  
}

#-----------------------------
# REQM
#-----------------------------

dados_REQM <- padroniza_metodo(tabela_larga) %>%
  mutate(indicador = "REQM")

#-----------------------------
# Erro bruto
#-----------------------------

dados_RB <- padroniza_metodo(tabela_larga_erro) %>%
  mutate(indicador = "Erro bruto")

#-----------------------------
# Viés relativo
#-----------------------------

dados_PB <- padroniza_metodo(tabela_larga_PB) %>%
  mutate(indicador = "Viés relativo")

#-----------------------------
# Junta tudo
#-----------------------------

dados_final <- bind_rows(
  dados_REQM,
  dados_RB,
  dados_PB
)



faz_grafico <- function(dados, titulo, legenda, titulo_x,titulo_y){

  ggplot(
    dados,
    aes(
      x = proporcao,
      y = metodo,
      fill = valor
    )
  ) +

    geom_tile(color = "white") +

    facet_wrap(~ mecanismo) +

    scale_fill_gradient2(
      low = "#B2182B",
      mid = "white",
      high = "#2166AC",
      midpoint = 0,
      name = legenda
    ) +

    labs(
      title = titulo,
      x = titulo_x,
      y = titulo_y
    ) +

    `theme_minimal(base_family = "Arial") +

    theme(
      axis.text.x = element_text(
        angle = 90,
        hjust = 0.5
      ),

      plot.title = element_text(
        hjust = 0.5
      )
    )
}


g_REQM <- faz_grafico(
  dados_REQM,
   titulo= "",
  "REQM",
  titulo_x= "",
  titulo_y =""
)

g_RB <- faz_grafico(
  dados_RB,
   titulo="",
  "Erro bruto",
  titulo_x= "",
  titulo_y = "Métodos de imputação"
)

g_PB <- faz_grafico(
  dados_PB,
   titulo ="",
  "Viés relativo",
  titulo_x = "Percentual de dados faltantes",
  titulo_y =""
)



g_final <- (
  g_REQM /
  g_RB /
  g_PB
)




# ggsave("Indicadores_TFTM.png",g_final,path = here(),width = 15,height = 15,units = "cm",dpi = 300)
```








